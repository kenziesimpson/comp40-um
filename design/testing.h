#include "interaction.h"

/*
 * test_new_memory_module
 *
 * Tests the new_memory_module function. Ensures that a new memory module is
 * created and that its members are correctly initialized.
 *
 * @param  void
 * @return void
 */
void test_new_memory_module(void);

/*
 * test_free_memory_module
 *
 * Tests the free_memory_module function. Ensures that the members of a created
 * memory module are correctly freed and set to NULL.
 *
 * @param  void
 * @return void
 */
void test_free_memory_module(void);

/*
 * test_new_segment
 *
 * Tests the new_segment function. Ensures that a s
 *
 * @param  void
 * @return void
 */
void test_new_segment(void);

/*
 * test_remove_segment
 *
 * Tests the remove_segment function. Ensures that a segment is removed and
 * that its members are correctly freed and set to NULL.
 *
 * @param  void
 * @return void
 */
void test_remove_segment(void);

/*
 * test_new_executor
 *
 * Tests the new_executor function. Ensures that a new executor is created and
 * that its members are correctly initialized.
 *
 * @param  void
 * @return void
 */
void test_new_executor(void);
